#!/usr/bin/env python3
"""
Test utilities for EXIF tool tests
"""

import os
import sys

# Add the parent directory to the path so tests can import from the main project
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
